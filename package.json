{
  "name": "ficha-de-ponto",
  "version": "0.1.0",
  "description": "Bah.",
  "main": "src-server/server.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node server/server.js",
    "start": "node server/server.js",
    "dev": "nodemon ./src-server/server.ts",
    "dev-start": "node --loader ts-node/esm ./src-server/server.ts"
  },
  "dependencies": {
    "express": "^4.17.2",
    "ts-node": "^10.5.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.6",
    "@types/express": "^4.17.13",
    "nodemon": "^2.0.15"
  },
  "engines": {
    "node": "16.x"
  },
  "type": "module",
  "author": "MaoDoBan",
  "license": "ISC",
  "nodemonConfig": {
    "ignore": ["referencias/*", "client/*"],
    "delay": 2500,
    "execMap": {
      "ts": "node --loader ts-node/esm"
    }
  }
}
